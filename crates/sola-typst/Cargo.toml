[package]
name = "sola-typst"
version = "0.1.0"
edition = "2021"

[dependencies]
typst = "0.13.0"
typst-svg = "0.13.0"
typst-assets = "0.13.0"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
